#include "main.h"

/**
 * _strlen - Returns the length of a string
 * @s: The string
 * Return: Length of @str
 */

int _strlen(char *s)
{
	int size;

	for (size = 0; *s++; size++)
	{
	}
	return (size);
}
