#include "main.h"

/**
 * _isdigit - Checks for 9 digit
 * @c: Tnt to be checked
 * Return: 1 if uppercase 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	return (0);
}
